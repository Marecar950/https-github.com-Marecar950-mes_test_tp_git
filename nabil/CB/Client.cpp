#include <iostream>
#include "Client.h"
#include "Compte.h"

using namespace std;


int Client (std::string Nom, std::string Prenom, int CIN, int Numero, int id, int Solde);
{
this->Nom = Nom;
this->Prenom = Prenom;
this->CIN = CIN;
this->Numero = Numero;
this->id = id;
this->Solde = Solde:   
}

void client::depot(int somme) 
{
    principale->crediter(somme);
}
    
return 0;